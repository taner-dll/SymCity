{% extends 'base.html.twig' %}

{% block title %}Koleksiyon Grubu Güncelle{% endblock %}

{% block body %}
    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Koleksiyon Grubu Güncelle</h3>
        </div>
        <div class="box-body">
            <div class="row">
                <div class="col-sm-6">
                    {{ include('collection_group/_form.html.twig') }}
                </div>
            </div>

        </div>
        <!-- /.box-body -->
        <div class="box-footer">
            <button class="btn btn-success" style="margin-right: 10px;">{{ button_label|default('Güncelle') }}</button>
            {{ form_end(form) }}


            <a href="{{ path('collection_group_show', {'id': collection_group.id}) }}" class="btn btn-primary"
               style="margin-right: 10px;">Geri Dön</a>
        </div>
        <!-- /.box-footer-->
    </div>
{% endblock %}
