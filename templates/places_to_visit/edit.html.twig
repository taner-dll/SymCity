{% extends 'base.html.twig' %}

{% block title %}Gezilecek Yerler | {{ places_to_visit.name }} (edit) {% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('places_to_visit') }}
{% endblock %}

{% block breadcrumb %}

    <section class="content-header">
        <h1>
            Gezilecek Yerler
            <small>{{ places_to_visit.name }} (edit)</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="{{ path('app_dashboard') }}">Ana Sayfa</a></li>
            <li><a href="{{ path('places_to_visit_index') }}">Gezilecek Yerler</a></li>
            <li class="active"><a href="{{ path('places_to_visit_edit', {id:places_to_visit.id}) }}">
                    {{ places_to_visit.name }} (edit)</a></li>

        </ol>
    </section>

{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <!--
                <div class="box-header with-border">
                    <h3 class="box-title"></h3>
                </div>-->
                <!-- /.box-header -->
                <!-- form start -->

                {{ include('places_to_visit/_form.html.twig') }}

                <div class="box-footer">
                    <a class="btn btn-primary" style="margin-right: 10px;"
                       href="{{ path('places_to_visit_show',{id:places_to_visit.id}) }}">Geri</a>
                    <button class="btn btn-success">{{ button_label|default('Güncelle') }}</button>

                </div>

                {{ form_end(form) }}

            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('places_to_visit') }}
{% endblock %}
