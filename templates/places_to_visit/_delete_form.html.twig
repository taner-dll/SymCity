<form method="post" action="{{ path('places_to_visit_delete', {'id': places_to_visit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ places_to_visit.id) }}">
    <button class="btn">Delete</button>
</form>
