{% extends 'base.html.twig' %}

{% block title %}Municipality{% endblock %}

{% block body %}
    <h1>Municipality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ municipality.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ municipality.name }}</td>
            </tr>
            <tr>
                <th>About</th>
                <td>{{ municipality.about }}</td>
            </tr>
            <tr>
                <th>Mayor</th>
                <td>{{ municipality.mayor }}</td>
            </tr>
            <tr>
                <th>Featured_picture</th>
                <td>{{ municipality.featuredPicture }}</td>
            </tr>
            <tr>
                <th>Web</th>
                <td>{{ municipality.web }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ municipality.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ municipality.phone }}</td>
            </tr>
            <tr>
                <th>Fax</th>
                <td>{{ municipality.fax }}</td>
            </tr>
            <tr>
                <th>Map</th>
                <td>{{ municipality.map }}</td>
            </tr>
            <tr>
                <th>Extra_info</th>
                <td>{{ municipality.extraInfo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('municipality_index') }}">back to list</a>

    <a href="{{ path('municipality_edit', {'id': municipality.id}) }}">edit</a>

    {{ include('municipality/_delete_form.html.twig') }}
{% endblock %}
