{% extends 'base.html.twig' %}

{% block title %}Gezilecek Yer Kategorisi{% endblock %}

{% block stylesheets %}
{{ parent() }}
{% endblock %}

{% block breadcrumb %}

<section class="content-header">
    <h1>
        Gezilecek Yer Kategorisi
        <small>{{ p_t_v_category.shortName }} (edit)</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="{{ path('app_dashboard') }}">Ana Sayfa</a></li>
        <li><a href="{{ path('p_t_v_category_index') }}">Gezilecek Yer Kategorisi</a></li>
        <li class="active"><a href="{{ path('p_t_v_category_edit',
        {'id': p_t_v_category.id}) }}">
                {{ p_t_v_category.shortName }} (edit)</a></li>

    </ol>
</section>

{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-12">
        <div class="box">
            <!--
            <div class="box-header with-border">
                <h3 class="box-title"></h3>
            </div>-->
            <!-- /.box-header -->
            <!-- form start -->

            {{ include('ptv_category/_form.html.twig') }}

            <div class="box-footer">
                <a class="btn btn-primary" style="margin-right: 10px;"
                   href="{{ path('p_t_v_category_show', {'id': p_t_v_category.id}) }}">Geri</a>
                <button class="btn btn-success">{{ button_label|default('Güncelle') }}</button>

            </div>

            {{ form_end(form) }}

        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
{% endblock %}