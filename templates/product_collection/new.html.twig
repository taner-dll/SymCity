{% extends 'base.html.twig' %}


{% block title %}Yeni Koleksiyon Alt Grubu Ekle{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Yeni Koleksiyon Alt Grubu Oluştur</h3>
                </div>
                <!-- /.box-header -->
                <!-- form start -->

                {{ include('product_collection/_form.html.twig') }}

                <div class="box-footer">
                    <button class="btn btn-success">{{ button_label|default('Kaydet') }}</button>
                    <a class="btn btn-primary pull-right" href="{{ path('product_collection_index') }}">
                        Koleksiyon Alt Grupları Listesi</a>
                </div>

            </div>
        </div>
    </div>
{% endblock %}
